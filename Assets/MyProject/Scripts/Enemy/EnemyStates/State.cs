using UnityEngine;

namespace MyProject.Scripts.Enemy.EnemyStates
{
    public class State : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
